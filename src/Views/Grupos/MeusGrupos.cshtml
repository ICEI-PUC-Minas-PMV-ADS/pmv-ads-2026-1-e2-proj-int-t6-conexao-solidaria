@model IEnumerable<ConexaoSolidaria.Models.GrupoApoio>
@{
    ViewData["Title"] = "Meus Grupos";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="cs-page-header mb-4">
    <div>
        <h1 class="cs-page-title">Meus Grupos</h1>
        <p class="cs-page-subtitle">Grupos de apoio dos quais você participa</p>
    </div>
    <div>
        <a asp-action="Criar" asp-controller="Grupos" class="btn btn-cs-primary">+ Novo Grupo</a>
    </div>
</div>

<div class="row">
    <div class="col-12 col-lg-8 mx-auto">
        @if (Model == null || !Model.Any())
        {
            <div class="text-center py-5 bg-white shadow-sm" style="border-radius:15px;">
                <span style="font-size:3rem;">👥</span>
                <h4 class="mt-3 text-muted">Você ainda não participa de nenhum grupo</h4>
                <p class="text-muted">Explore os grupos disponíveis ou crie um novo.</p>
                <a asp-action="Lista" asp-controller="Grupos" class="btn btn-cs-primary mt-2">
                    Ver grupos disponíveis
                </a>
            </div>
        }
        else
        {
            foreach (var grupo in Model)
            {
                <a href="/Grupos/Detalhes/@grupo.Id" class="text-decoration-none text-dark">
                    <div class="card mb-3 shadow-sm border-0 cs-solic-card">
                        <div class="card-body d-flex align-items-center">
                            <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center"
                                 style="width:50px;height:50px;font-size:20px;font-weight:bold;">
                                @(!string.IsNullOrEmpty(grupo.Nome) ? grupo.Nome.Substring(0, 1).ToUpper() : "G")
                            </div>
                            <div class="ms-3 flex-grow-1">
                                <h6 class="mb-0 fw-bold">@grupo.Nome</h6>
                                <div class="text-truncate text-muted small" style="max-width:400px;">
                                    @grupo.Descricao
                                </div>
                            </div>
                            <div class="ms-2"><span class="text-primary">→</span></div>
                        </div>
                    </div>
                </a>
            }
        }
    </div>
</div>